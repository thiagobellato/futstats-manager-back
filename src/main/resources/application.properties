spring.profiles.active=${APP_PROFILE:local}

# JWT (fallback; sobrescrito por profile)
app.jwt.secret=${APP_JWT_SECRET:FutStatsManagerLocalDevSecretKey2026Secure!!}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}