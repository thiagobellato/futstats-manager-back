package br.com.bellato.gerenciador_fifa.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS é configurado em {@link SecurityConfig#corsConfigurationSource()}
 * para integração correta com Spring Security (preflight OPTIONS + credentials).
 * Esta classe permanece como marcador para não quebrar imports/documentação legada.
 */
@Configuration
public class CorsConfig {
}
